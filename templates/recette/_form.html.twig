<!-- formulaire Ajouter nouvelle recette -->
{{ form_start(form, {'attr': {'class': 'text-center border border-light p-5'}} ) }}
        {# <div class="col mb-5">
            <div class="card mx-auto p-3 mt-5"> #}
                <div class="form-group">
                    <label for="photo">Choisir une photo:</label>
                     {{ form_widget(recette.photo, {'attr': {'class': 'form-control-file','placeholder': 'Illustration'}}) }}
                </div>

                <div class="form-group">
                    <label for="title">Nom du plat:</label>
                    {{ form_widget(recette.title, {'attr': {'class': 'form-control mb-4','placeholder': 'Titre du plat'}}) }}
                </div>

                <div class="form-group">
                    <label for="diet">Régime alimentaire</label>
                    {{ form_widget(recette.diet, {'attr': {'class': 'form-control mb-4','placeholder': 'Prénom'}}) }}
                </div>
                <div class="form-group">
                    <label for="category">Catégorie</label>
                    {{ form_widget(recette.category, {'attr': {'class': 'form-control mb-4','placeholder': 'Catégorie'}}) }}
                </div>
                
                <button type="submit" class="btn btn-primary">Ajouter ma recette</button>
    
{{ form_end(form)}}
